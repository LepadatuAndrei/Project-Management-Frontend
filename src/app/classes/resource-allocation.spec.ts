import { ResourceAllocation } from './resource-allocation';

describe('ResourceAllocation', () => {
  it('should create an instance', () => {
    expect(new ResourceAllocation()).toBeTruthy();
  });
});
